[package]
name = "verifier"
version = "0.1.0"
edition = "2024"

[dependencies]
diagnostics = { path = "../diagnostics" }
parser = { path = "../parser" }
semantics = { path = "../semantics" }
z3 = "0.20.0"

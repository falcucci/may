[package]
name = "may"
version = "0.1.0"
edition = "2024"

[workspace]
members = [
  ".",
  "crates/parser",
]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
parser = { path = "crates/parser" }
